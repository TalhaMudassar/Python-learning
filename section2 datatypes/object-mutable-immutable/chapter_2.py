#immutable 
specie_mix = set()
print(specie_mix)
print(f"initial specie_mix id are {id(specie_mix)}")

specie_mix.add("Ginger")
specie_mix.add("garlic")
print(specie_mix)
print(f"after adding , specie_mix id are {id(specie_mix)}")
